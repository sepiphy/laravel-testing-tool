version: "3.3"

services:
  php7.2:
    build:
      context: ./docker/php7.2
      args:
        - CODE_GID=${CODE_GID}
        - CODE_UID=${CODE_UID}
    volumes:
      - ${LARAVEL_FRAMEWORK_DIR}:/app
      - $PWD/phpunit.laravel.xml:/app/phpunit.xml
